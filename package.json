{
  "name": "fullstack-app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix src/frontend run dev\" \"npm --prefix src/backend run dev\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
